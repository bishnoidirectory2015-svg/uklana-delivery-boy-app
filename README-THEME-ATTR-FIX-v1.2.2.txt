Uklana Delivery Boy App v1.2.2

Build fix:
- Removed unsupported windowShowWhenLocked/windowTurnScreenOn style items.
- Added API-safe screen wake and lock-screen handling inside OrderAlertActivity.
- Ring stops immediately on Accept or Forward.

Copy all project files into the existing GitHub repository, but keep the existing .git folder. Commit and push to run GitHub Actions again.
