Uklana Delivery Boy App v1.2.1

Fix:
- Added the missing Theme.UklanaRestaurant Android style.
- Added alert theme support for lock-screen/full-screen order alerts.
- Resolves GitHub Actions AAPT error: resource style/Theme.UklanaRestaurant not found.

GitHub update:
Copy the files from this package into the existing repository and overwrite matching files.
Keep the existing .git and .github folders.
Commit and push; GitHub Actions will start a new build automatically.
